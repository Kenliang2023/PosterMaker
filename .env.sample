# =================== 应用配置 ===================
# 服务器端口
PORT=3001

# 环境设置: development | test | production
NODE_ENV=development

# Render部署标识，在Render环境应设为true
RENDER=false

# 日志级别控制
VERBOSE=true         # 一般日志详细输出
DB_VERBOSE=false     # 数据库操作日志详细输出

# =================== 存储配置 ===================
# 存储根目录 - 本地开发时使用项目根目录下的data文件夹
# Render环境应设置为 /tmp
# STORAGE_DIR=./data

# =================== API密钥 ===================
# Gemini API - 需要替换为实际的API密钥
GEMINI_API_KEY=your_api_key_here
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1/models/gemini-2.0-flash-exp:generateContent

# =================== 安全配置 ===================
# 生产环境中应使用强随机字符串
JWT_SECRET=dev_jwt_secret
SESSION_SECRET=dev_session_secret

# =================== 文件上传配置 ===================
# 10MB in bytes
MAX_FILE_SIZE=10485760

# =================== 部署说明 ===================
# 本地开发：
# - 复制此文件为 .env
# - 配置 GEMINI_API_KEY
# - 保持其他默认值
#
# Render部署：
# - 不需要此文件
# - 在Render环境变量中设置:
#   NODE_ENV=production
#   RENDER=true
#   GEMINI_API_KEY=您的API密钥
#   JWT_SECRET=随机字符串
#   SESSION_SECRET=随机字符串 